<!-- NAV BAR -->
  <header>
    <div class='container'>
      <nav class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Creepr</a>
          </div>
          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li class="active"><a href="/about">About</a></li>
            </ul>
            <!-- Search BY Tag -->
            <form class="navbar-form navbar-left" role="search" method="get" action="/photos/tag">
              <div class="form-group">
                <input type="text" class="form-control" placeholder="Search by Tag" name="tags" required>
              </div>
              <button type="submit" class="btn btn-default" value="filter">Search</button>
            </form>
            <ul class="nav navbar-nav navbar-right">
              <li><a href="/photos">Home</a></li>
              <li><a href="/photos/new">Add Photo</a></li>
              <li class="dropdown">
              <!--Popular tags -->
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Popular Tags<span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <%tags = Tag.all%>
                  <% tags.sort_by!{|tag| tag.photos.size} %>
                  <% t1 = tags.pop.name %>
                  <% t2 = tags.pop.name %>
                  <% t3 = tags.pop.name %>
                  <li><a href='/photos/tag/<%= t1 %>'><%= t1 %></a></li>
                  <li><a href='/photos/tag/<%= t2 %>'><%= t2 %></a></li>
                  <li><a href='/photos/tag/<%= t3 %>'><%= t3 %></a></li>
                </ul>
              </li>
            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>
    </div>
  </header>

<% if session[:user_id] %>
  oops, <%=User.find(session[:user_id])[:username] %>
<a href="/logout">Logout</a>
<% else %>
  <h1>Log in here!</h1>
  <% if @error %>
     Incorrect credentials 
  <% end %>

  <form id="signup-form" method="get" action="/signup">
  </form>

<form id="login-form" method="post" action="/login">
  <div class="row" style='padding: 25px;'>
    <div class="col-md-3 col-xs-12">
      <label>Username:</label>
        <input type="text" name="username" class="form-control">
    </div>
    <div class="col-md-3 col-xs-12">
      <label>Password:</label>
        <input type="password" name="password" class="form-control">
    </div>
  </div>
</form>
<div class="row" style="padding:25px;">
  <div class="col-md-3 col-xs-12">
    <button form="login-form"type="submit" class='btn btn-danger'>Login</button>
    <button form="signup-form" type="submit" class='btn btn-danger'>Sign Up</button>
  </div>
</div>

<%end%>