<div>
  <a href="/photos"> Back </a>
</div>


<% logged_user = User.find(session[:user_id]) %>
<h3>PROFILE FOR : <%= logged_user.username%></h3>
<p>
Tags subscribed to: 

<%logged_user.tag_subscriptions.each do |tag_sub|%>
  <%= Tag.find(tag_sub.tag_id).name %>
<% end %>
</p>
<p>
  <% total = 0 %>
  <% logged_user.photos.each do |photo| %>
    <% total += photo[:rating] %>
  <% end %>
  total number of stars: <%=total%>
</p>
<form method="post" action="/addYOusername">
  YO Username: <small><%=logged_user.yo_username%></small>
  <div>
  <input type="text" name="YOname" >
  </div>
  <button type='submit'>save YO username</button>
</form>

Photo's I've posted:
<!-- MAIN PHOTO DISPLAY -->
<p>
<% @user_photos.reverse! %>
<div class="container">
  <% @user_photos.each_slice(3).to_a.each do |photo_row| %>
    <div class="row borderz">
    <% photo_row.each do |photo| %>
     <div class="col-xs-12 col-sm-6 col-md-4">
      <a href='/photos/<%= photo.id %>' style='background-image: url(/uploads/<%= photo.filename%>);' class="thumb img-responsive img-thumbnail" alt="Responsive image"/></a>
      <div class='row'>
      <div class='rating-text'><%=photo.rating%></div>
        <div class='comment-text'>     
          <form method="post" action="/delete_photo">
          <input type="hidden" name="photo_id" value="<%=photo.id%>">
          <input type="hidden" name="user_name" value="<%=logged_user.username%>">
          <button type='submit'>delete</button>
          </form>
        </div>
      </div>
    </div>
    <% end %>
  </div>
 <% end %>
</div>



</p>
